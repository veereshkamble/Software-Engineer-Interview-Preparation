package LinkedList;

public class FlattenMultiLevelDoublyLinkedList {

    public Node flatten(Node head) {
        Node cur = head;
        while(cur != null) {
            if(cur.child != null) {
        }
    }
}
